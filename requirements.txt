# Runtime dependencies — Vercel installs exactly this list for api/index.py.
# Keep local-only tooling in requirements-dev.txt so it never reaches the function bundle.
fastapi>=0.115
python-multipart>=0.0.9
google-genai>=1.30
firebase-admin>=6.5
python-dotenv>=1.0
openpyxl>=3.1
python-docx>=1.1
pdfplumber>=0.11
